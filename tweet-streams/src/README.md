# The simplest of twitter bots.

Just "listens" to given trackword and outputs it to a folder.

### TODO:

- Make it an executable maybe? accepting parameters?
- Use loki.js or something similar instead of plain json files.
