## Densify GPX track and embed Health Data in FIT

This is not meant to be redistributable. it's only a script I used to granularize
a GPX I created.

### Usage

Just install dependency and run `python src`
